import { action } from '@storybook/addon-actions' import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks' import { Button } from './Button'

Button
With MDX we can define a story for Button right in the middle of our Markdown documentation.

{args => }
